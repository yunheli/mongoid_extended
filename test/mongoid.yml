development:
  sessions:
    default:
      database: mongoid_extended_development
      hosts:
        - localhost:27017
      options:
  options:
    raise_not_found_error: false
    use_utc: true
test:
  sessions:
    default:
      database: mongoid_extended_test
      hosts:
        - localhost:27017
      options:
        read: primary
        max_retries: 1
        retry_interval: 0
